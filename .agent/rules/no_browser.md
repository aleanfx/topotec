# Rule: No Browser Subagent

- **CRITICAL**: Do NOT execute the `browser_subagent` tool unless the USER explicitly requests a visual verification or a browser-based task.
- **Reasoning**: The user finds the browser agent slow and distracting ("me retrasa mucho").
- **Alternative**: Rely on code analysis and manual verification instructions for the user when possible.
